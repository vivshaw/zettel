---
tags: ml, LLMs, neural networks
---

- the basic mechanism that launched the LLM revolution.
- in attention, the model uses a learned feature selection mechanism to determine which inputs to "pay attention to" and where.
- started with [[RNN]]s and [[sequence-to-sequence]] tasks, later grew to [[CNN]]s
- but the [[transformer]] was the real turning point, using scaled dot-product attention